# Optional layers. Not needed for the AFML pipeline, the OpenBB data
# layer, the backtester, the Streamlit dashboard, or the offline test
# suite (pytest -m "not slow and not integration").
# The integration-marked tests in tests/test_api.py skip themselves
# unless fastapi and httpx below are installed.

# ── REST layer (api/server.py) ───────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0

# ── RAG knowledge base (analysis_engine/knowledge.py) ────────────────
chromadb>=0.4.22
langchain-community>=0.0.20
langchain-google-genai>=1.0.0
google-generativeai>=0.4.0
google-cloud-aiplatform>=1.42.0
ebooklib>=0.18
tqdm>=4.66.0

# ── Document ingestion for the RAG layer (PDF) ───────────────────────
PyPDF2>=3.0.0
